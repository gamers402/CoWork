CoWork:
----
I didn't feel like paying for CoWork, so i made my own. Set your API Key on windows PowerShell like this: setx GEMINI_API_KEY "your-key-here". Gemini is the best free API key i could find, thus it is the only model i can confirm to work.
--
made by NC (ECE Student in UB)
--
requirements: you need to pip install ___ like a million things. look at the python imports or just wing it. also you MUST put in your path in run.bat, it's obvious where. i suggest putting CoWork in your desktop.
--
suggestions: i suggest you run this via the run.BAT. also maybe put the transparency in the CMD somewhat transparent that seems to help. one more thing, there's a mouse auto hider i use but it actually decreases the AI accuracy so if you have something similar then turn it off.
--
details: the AI works perfectly fine on a dim screen as the screenshots take the raw pixel values. also this program will obviously not upload the screenshots to some server for me to view, i have better things to do and not the knowledge on how I'd even attempt to do that.
--
copyright: i don't know if CoWork is copyright, if this code suddenly disappears it's because i may be sued and would not like to be broke.
====
Explanation:
----
once the AI is done with it's task it'll type PURGE and restart itself.
--
memory is a temporary memory that saves the past 3 messages from the AI with a timestamp.
--
persistent memory is permanent memory that the AI will use for more complex tasks for planning
--
both of these are deleted upon PURGE.
--
the screenshot you see in the CoWork folder is what the AI sees and responds to
----
